package com.myspace.spectrum;

import java.lang.Number;

rule "Valet Services"
	dialect "mvel"
	when
		Patient( isPregnant == true , hasDisability == true , age > 65 , appointment.facility.hasValet == true , appointment.date == ( $today ) , hasUsedPortal == true , $today : appointment.today)
	then
		insert( new NotificationOfValetServices() );
end

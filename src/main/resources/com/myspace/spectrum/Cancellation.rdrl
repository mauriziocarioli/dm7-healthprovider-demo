package com.myspace.spectrum;

import java.lang.Number;

rule "Cancellation"
	dialect "mvel"
	when
		Patient( appointment.scheduled == true , hasUsedPortal == true , isAdmitted == true )
	then
		CancellationOffer $cancellation = new CancellationOffer();
		insert( $cancellation );
end
